export interface ILatLngQuery {
    lat: string;
    lng: string;
}

export interface ICodeQuery {
    code: string;
}

export interface ISlugQuery {
    slug: string;
}

export interface IWikiQuery {
    country: string;
    capital: string;
}

export interface IBoandaryQuery {
    north: number;
    south: number;
    east: number;
    west: number;
}

